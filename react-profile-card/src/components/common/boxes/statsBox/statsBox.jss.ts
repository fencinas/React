import { createUseStyles } from "react-jss";

export const StatsBoxStyle = createUseStyles(() => ({
   box: {
      display: "flex",
      flexDirection: "column",
      alignItems: "center",
   },

   top: {

   },

   bottom: {

   }
}))